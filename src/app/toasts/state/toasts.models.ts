import { Toast } from '../shared/toast.types';

export { Toast } from '../shared/toast.types';

/**
 * Interface for the 'Toasts' data
 */
export interface ToastsEntity extends Toast {
  id: number;
}
